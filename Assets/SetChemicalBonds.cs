using UnityEngine;
using System.Collections;

public class SetChemicalBonds : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	    // 60フレームごとに関数を呼び出す
	}
    // 原子の距離を判定して結合させるメソッド
}
